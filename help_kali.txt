sudo apt update
sudo apt install python3 python3-pip

python3 --version
pip3 --version

ساخت محیط مجازی تا تداخل در پکیج های سیستمی ایجاد نشه:

python3 -m venv ~/aioenv 
source ~/aioenv/bin/activate

pip install aiohttp aiohttp_socks


پوشه proxy_finder باز کنید:

open terminal here

فعال سازی محیط مجازی قبل اجرا فایل py. اگر فعال نبود:

source ~/aioenv/bin/activate

python3 proxy_finder_kali.py
